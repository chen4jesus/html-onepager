version: '3'
services:
  web:
    build: .
    ports:
      - "80:80"
      - "443:443"
    environment:
      - DOMAIN_NAME=${DOMAIN_NAME:-localhost}
      - DOMAIN_EMAIL=${DOMAIN_EMAIL:-admin@localhost}